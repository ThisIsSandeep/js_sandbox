let val;

val = document; // It will give whole document
val = document.all;
val = document.all[0];
val = document.all.length;
val = document.body;
val = document.doctype;
val = document.domain;
val = document.URL;
val = document.characterSet;
val = document.contentType;

// We can select things without select but it's not recommended
val = document.title;

console.log(val);
